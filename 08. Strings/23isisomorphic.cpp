//a given string is isomorphic or not 
#include<iostream>
#include<string>
#include<algorithm>
#include <vector>
#include <sstream>

using namespace std;
int main (){
    vector<string> v(150,1000);
    string s = "paper",t ="title";
    bool flag = false;
    if(s.length()!=t.length()) flag= false;
    for(int i=0;i<s.length();i++){
        int idx = (int)s[i];

    }
    
    
}